name: Deploy to EC2 IIS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Test SSH Connection
      run: |
       echo "Testing basic SSH connection..."
       ssh -v -i ~/.ssh/id_rsa Administrator@18.61.41.238 echo "Connection successful"
