name: 🚀 Deploy HTML Website to Windows Server

on:
  push:
    branches:
      - main  # Change if using a different branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v3

    - name: 📂 Deploy Files to Windows Server via PowerShell
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.WINDOWS_HOST }}
        username: ${{ secrets.WINDOWS_USERNAME }}
        password: ${{ secrets.WINDOWS_PASSWORD }}
        port: 5533
        script: |
          $source = "${{ github.workspace }}"
          $target = "D:\Kalyanbabu\UPRERA\APCTD\Beta3"
          Write-Host "Starting deployment..."
          Copy-Item -Path "$source\*" -Destination "$target" -Recurse -Force
          Write-Host "✅ Deployment completed successfully!"

